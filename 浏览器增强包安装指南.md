# 浏览器增强包安装指南

这份文档只解决一件事：**怎么让「HTTP 快速模式 / 隐身引擎 / 动态引擎 /
自适应选择器」真正能用起来。**

默认的「浏览器引擎」不需要看这份文档 —— 它开箱即用。

> 下面出现的 `<程序目录>` 一律指 **`SmartCrawler.exe` 所在的那一层**，例如
> `D:\Tools\SmartCrawler\`。免安装版解压出来就是这个结构：
>
> ```
> <程序目录>\
> ├── SmartCrawler.exe
> ├── _internal\
> ├── ms-playwright\          ← 放浏览器的地方（安装包已预留）
> └── crawler_data\           ← 首次运行后自动生成
>     └── site-packages\      ← 放 scrapling 包的地方
> ```

---

## 0. 先看你要哪种引擎，别多装

| 引擎 | 要 scrapling 包 | 要浏览器（约 706 MB） |
| --- | --- | --- |
| 浏览器引擎（默认） | 不要 | 不要 |
| HTTP 快速模式 | **要** | 不要 |
| 自适应选择器 | **要** | 不要 |
| 隐身引擎 `StealthyFetcher` | **要** | **要** |
| 动态引擎 `DynamicFetcher` | **要** | **要** |

**只装包就已经能用上 HTTP 快速模式与自适应选择器**，这一步只有几十 MB。
浏览器只有隐身 / 动态两个引擎才需要，所以别一上来就下 700 MB。

界面上会按实际就绪程度分三种说法，照着做就行：

| 界面提示 | 意思是 | 你要做 |
| --- | --- | --- |
| 未检测到 Scrapling | 步骤 1 没做 | 做步骤 1 |
| Scrapling 已就绪：HTTP 快速模式与自适应选择器可用 | 步骤 1 做了，缺浏览器 | 做步骤 2（只在要用隐身 / 动态引擎时） |
| Scrapling 已就绪，四种引擎均可用 | 都齐了 | 什么都不用做 |

左侧面板的 **「复制安装命令」** 按钮会把当前该用的命令复制到剪贴板，
直接粘进 PowerShell 回车即可，不必手打那串长路径。
左上角的 **「设置」** 按钮里有同样的指引与两个「打开目录」按钮，
还有 **「④ 引擎文件位置」**：包和浏览器装到别的盘（或 playwright 默认下到了
用户目录）时，在这里指过去就行，**不用移动任何文件**。

浏览器有三条路线，按网络情况挑一条即可：

| 路线 | 做法 | 适合 |
| --- | --- | --- |
| A（推荐） | 解压 Release 里的「浏览器增强包」 | 大部分情况，不用命令行 |
| B | 让 playwright 自己联网下载（官方源） | 网络通畅 |
| C | 从 npmmirror 镜像手动下载两个 zip | 官方源慢 / 不通 |

---

## 1. 装 scrapling 包（几十 MB，两种用法都建议做）

### 1.1 免安装版（`SmartCrawler.exe`）

程序**有意不打包** scrapling，但会把下面这个目录加进自己的 `sys.path`：

```
<程序目录>\crawler_data\site-packages\
```

所以只要把包装到那里就行。**必须用 Python 3.13.x**，理由见步骤 1.3。

```powershell
python -m pip install --target "<程序目录>\crawler_data\site-packages" "scrapling[fetchers]"
```

### 1.2 源码运行

```powershell
# 本项目的 venv 是 --without-pip 建的，用系统 pip 指定目标解释器。
# ⚠️ --python 必须放在 install 之前，否则报
#    The --python option must be placed before the pip subcommand name
python -m pip --python "<项目>\.venv\Scripts\python.exe" install "scrapling[fetchers]"
```

### 1.3 为什么是 `[fetchers]`、为什么是 3.13

- **`[fetchers]` 而不是 `[all]`**：scrapling 0.4.15 的 `all = ai,shell`，
  这两个 extra 会额外拉进 `mcp` / `IPython` / `markdownify`（连依赖树约 56 MB）。
  本项目只用四个引擎，四个引擎要的依赖（`curl_cffi` / `patchright` /
  `playwright`，以及 `scrapling.exe` 要的 `click`）**全在 `fetchers` 里**。
- **3.13 是免安装版的约束，不是 scrapling 的**：scrapling 自己要求 `>=3.10`。
  但外挂目录里装的是带 C 扩展的包（`curl_cffi` / `greenlet` / `lxml`），
  必须与 **exe 内置的那套运行时**同一次版本，否则导入就报 ABI 不匹配。
  免安装版内置的是 **Python 3.13**，所以这里要用 3.13.x 的 Python。
  源码运行则用哪个版本的解释器就用哪个版本，不受这条限制。

装完重启程序，界面提示会从「未检测到 Scrapling」变成下一个状态。

---

## 2. 装浏览器（约 706 MB，只有隐身 / 动态引擎需要）

### 路线 A（推荐）：解压「浏览器增强包」

从 Release 附件下载 **`SmartCrawler-v0.0.4-win64-browsers.zip`**，
把里面的 `ms-playwright` 文件夹解压到 `<程序目录>\`（和 `SmartCrawler.exe` 同一层）。

安装包里**已经预留了这个空文件夹**，直接覆盖进去即可，**不需要设任何环境变量**。

解压完成后应该是这个结构（**一个条目一行**，标出哪些必需）：

```
<程序目录>\ms-playwright\
├─ chromium-1243\
│  └─ chrome-win64\
│     ├─ chrome.exe              ← 必需（完整 Chromium）
│     └─ 其它 dll / pak / locales…
├─ chromium_headless_shell-1243\
│  └─ chrome-headless-shell-win64\
│     ├─ chrome-headless-shell.exe   ← 必需（默认就它启动）
│     └─ 其它 dll / pak…
├─ ffmpeg-1011\                  ← 可选（只有录像时才用）
├─ winldd-1007\PrintDeps.exe     ← 必需（缺了会报依赖校验失败）
└─ 浏览器增强包说明.txt          ← 说明文件，留着不影响
```

**判据：上面两个 `.exe` 都在，引擎就绪** —— 程序只看这两个文件，不看目录大小。
（也可以在程序里点「设置 → 刷新状态」，或看左侧引擎提示。）

- **不要多套一层**：解压成 `ms-playwright\chromium-1243\chrome-win64\chrome-win64\`
  是最常见的错误，playwright 会当作「没安装」而重新去下载。
- 目录名里的 `1243`、`1011`、`1007` 是 playwright / patchright **1.63.0**
  要求的版本号，不要改动大小写与拼写。
- `chromium` 与 `chromium_headless_shell` 两个都要：隐身引擎默认 `headless=True`，
  走的是 headless shell；只有 `headless=False` 才用完整 chromium。
- 浏览器**不必**放在程序目录：不想占程序盘的话，解压到任意位置，
  然后在程序里 **「设置 → ④ 引擎文件位置 → 浏览器目录」** 指过去即可
  （也可以在安装前先改，程序会按你指定的落点提示）。

重开程序，提示会变成「四种引擎均可用」。

### 路线 B：联网自行下载（playwright 官方源）

**源码运行**（控制台脚本就在项目 `.venv` 里，路径本来就通）：

```powershell
<项目>\.venv\Scripts\scrapling.exe install
```

**免安装版必须多设两个环境变量**，否则这条命令会直接
`ModuleNotFoundError: No module named 'scrapling'`：

```powershell
$sp = "<程序目录>\crawler_data\site-packages"

# ① 必须：pip --target 装出来的控制台脚本，启动时 sys.path[0] 是 Scripts\ 那一层，
#    不含外挂目录，所以脚本自己 import 不到 scrapling。
#    程序本体的 sys.path 里有它，但那是程序进程的事，管不到你新起的进程。
$env:PYTHONPATH = $sp

# ② 可选但推荐：指定浏览器落点。
#    不设的话浏览器会下到 %LOCALAPPDATA%\ms-playwright（见下方"下到哪了"）。
$env:PLAYWRIGHT_BROWSERS_PATH = "<程序目录>\ms-playwright"

& "$sp\Scripts\scrapling.exe" install
```

#### 下到哪了？先问 playwright 自己

联网下载的默认落点是**用户目录**，不是程序目录：

```
C:\Users\<你的用户名>\AppData\Local\ms-playwright
```

装完在程序目录里找不到浏览器是正常的。想知道**这次到底会下到哪、下哪个版本**，
先干跑一次（不会真的下载）：

```powershell
# 源码运行：用项目里的 playwright
<项目>\.venv\Scripts\python.exe -m playwright install --dry-run

# 免安装版：先让外挂目录可见，再用系统 Python 问它
$env:PYTHONPATH = "<程序目录>\crawler_data\site-packages"
python -m playwright install --dry-run
```

输出里有两行关键信息：

```
Install location : C:\Users\<你>\AppData\Local\ms-playwright\chromium-1243
Download url     : https://cdn.playwright.dev/builds/cft/153.0.8010.12/win64/chrome-win64.zip
```

**不用为了程序去搬这些文件**：打开程序 →「设置 → ④ 引擎文件位置 →
浏览器目录」选到 `...\AppData\Local\ms-playwright`，程序就会直接用那里
（改完立即生效，不必重启）。

> 这两条 `$env:` 只在**当前这个 PowerShell 窗口**有效，关掉就没了 ——
> 这是一次性的安装操作，**不需要**设成永久环境变量。
>
> 不要写 `python -m scrapling install`：scrapling 包里没有 `__main__.py`，
> 那样写会报 `No module named scrapling.__main__`。
> （`python -m playwright` 不受此限，它是可用的。）

### 路线 C：国内镜像手动下载（npmmirror，官方源太慢时用）

官方 CDN 在国内经常只有几十 KB/s。npmmirror 上有同一份文件，路径如下
（**本安装包对应的版本是 `153.0.8010.12`，revision `1243`**）：

1. 打开镜像页面（人点着方便）：

   <https://registry.npmmirror.com/binary.html?path=playwright/builds/cft/153.0.8010.12/>

   进去 `win64\` 目录，下载这两个文件（也可以直接用后面的直链）：

   | 文件 | 直链 |
   | --- | --- |
   | `chrome-win64.zip`（约 196 MB） | `https://cdn.npmmirror.com/binaries/playwright/builds/cft/153.0.8010.12/win64/chrome-win64.zip` |
   | `chrome-headless-shell-win64.zip`（约 115 MB） | `https://cdn.npmmirror.com/binaries/playwright/builds/cft/153.0.8010.12/win64/chrome-headless-shell-win64.zip` |

   > 同一份文件还有一条 Chrome for Testing 的镜像路径，任选其一：
   > `https://cdn.npmmirror.com/binaries/chrome-for-testing/153.0.8010.12/win64/chrome-win64.zip`
   >
   > 版本号请用上面 `--dry-run` 输出的那个（换过 playwright 版本时会不同），
   > 把 URL 里的 `153.0.8010.12` 替换掉即可。

2. 解压并归位（目录名**必须一模一样**，多套一层等于没装）：

   ```
   chrome-win64.zip                → <程序目录>\ms-playwright\chromium-1243\chrome-win64\
   chrome-headless-shell-win64.zip → <程序目录>\ms-playwright\chromium_headless_shell-1243\chrome-headless-shell-win64\
   ```

3. 每个 `chromium*` 目录里建**两个空标记文件**（缺了它们，playwright 会把
   这份手动装的当成「没安装」重新下载）：

   ```powershell
   $r = "<程序目录>\ms-playwright"
   New-Item -ItemType File -Force "$r\chromium-1243\INSTALLATION_COMPLETE"
   New-Item -ItemType File -Force "$r\chromium-1243\DEPENDENCIES_VALIDATED"
   New-Item -ItemType File -Force "$r\chromium_headless_shell-1243\INSTALLATION_COMPLETE"
   New-Item -ItemType File -Force "$r\chromium_headless_shell-1243\DEPENDENCIES_VALIDATED"
   ```

4. （建议）`DEPENDENCIES_VALIDATED` 只顶 30 天，过期后 playwright 会重跑依赖
   校验，那时需要 `winldd-1007\PrintDeps.exe`（只有 128 KB）。顺手也拿一下：

   <https://registry.npmmirror.com/binary.html?path=playwright/builds/winldd/1007/>

   解压成 `<程序目录>\ms-playwright\winldd-1007\PrintDeps.exe`。

5. 回到程序点「设置 → 刷新状态」，显示「隐身 / 动态引擎：可用」即成功。

> **为什么不能设个环境变量让 playwright 自己走镜像？**
> playwright 1.58 起 Chromium 改用 Chrome for Testing，下载路径
> `builds/cft/<版本>/<平台>/…` 是**硬编码**在源码里的，而
> `PLAYWRIGHT_DOWNLOAD_HOST` 只替换域名部分 —— 拼到镜像上就是 404。
> 所以这条路只能手动下；不想动手就走路线的 A（解压增强包）。

### 路线 B 的备选：自己下载压缩包手动放

网络下载经常超时（连国内镜像也可能缺对应版本），这时**直接手动下载最快**：

1. 打开 Chrome for Testing 官方下载页
   <https://googlechromelabs.github.io/chrome-for-testing/#stable>
2. 下载这两个 **win64** 包（本安装包用的是 `153.0.8010.12`）：
   - `chrome-win64.zip`
   - `chrome-headless-shell-win64.zip`
3. 解压并**重命名/归位**成路线 A 里那个结构：
   - `chrome-win64.zip` 里的 `chrome-win64\` → `<程序目录>\ms-playwright\chromium-1243\chrome-win64\`
   - `chrome-headless-shell-win64.zip` 里的 `chrome-headless-shell-win64\`
     → `<程序目录>\ms-playwright\chromium_headless_shell-1243\chrome-headless-shell-win64\`
4. 两个浏览器目录里各建两个**空文件** `INSTALLATION_COMPLETE` 与
   `DEPENDENCIES_VALIDATED`（没有它们 playwright 会认为没装过，又去下载）。
5. `ffmpeg-1011\` 与 `winldd-1007\` 如果还在就**别删**，删了会重新下载；
   缺 `winldd-1007` 时 Windows 上启动浏览器可能直接报错。

> playwright **不校验**浏览器二进制的具体版本，只认目录名与可执行文件位置。
> 但版本对不上可能在个别站点上表现异常，能用路线 A 就别自己配。

---

## 3. 验证

### 3.1 界面

重开程序，看左侧「② 抓取引擎（反检测）」下面的提示是否为
「Scrapling 已就绪，四种引擎均可用」。

### 3.2 命令行自检

```powershell
<程序目录>\SmartCrawler.exe --selftest
```

### 3.3 让 scrapling 自己确认浏览器找得到

**源码运行**：

```powershell
<项目>\.venv\Scripts\scrapling.exe install
```

**免安装版**（同样要先设 `PYTHONPATH` 与 `PLAYWRIGHT_BROWSERS_PATH`，见路线 B）：

期望输出里出现 `The dependencies are already installed` 与正确的
`Install location`（指向你的 `ms-playwright`），**而不是**又去下载。

### 3.4 真的启动一次浏览器

**源码运行**：

```powershell
<项目>\.venv\Scripts\python.exe -c "from playwright.sync_api import sync_playwright; p=sync_playwright().start(); b=p.chromium.launch(headless=True); pg=b.new_page(); pg.goto('https://example.com'); print('OK:', pg.title()); b.close(); p.stop()"
```

**免安装版**（装好包之后，前面先设好 `PYTHONPATH`）：

```powershell
python -c "import sys; sys.path.insert(0, r'<程序目录>\crawler_data\site-packages'); from playwright.sync_api import sync_playwright; p=sync_playwright().start(); b=p.chromium.launch(headless=True); pg=b.new_page(); pg.goto('https://example.com'); print('OK:', pg.title()); b.close(); p.stop()"
```

能打印出标题，就说明这套环境彻底通了。

---

## 4. 注意事项

1. **`--python` 的位置**：`pip install --python ...` 报
   `The --python option must be placed before the pip subcommand name`。
   正确写法是 `pip --python ... install ...`。
2. **`EPERM: operation not permitted`（写 `D3DCompiler_47.dll` 失败）**：
   十有八九是杀毒软件 / Windows Defender 的实时防护在拦。
   把浏览器目录挪出系统盘、并把它加入杀软白名单即可；
   **不要**为了装浏览器长期关闭杀软，装完记得开回来。
3. **下载超时**（`Request to https://cdn.playwright.dev/... timed out`）：
   走路线 A（解压增强包）或路线 C（国内镜像手动下载）。
   ⚠ 不要指望 `PLAYWRIGHT_DOWNLOAD_HOST` 指向镜像就能解决 ——
   playwright 1.58 起 Chromium 的下载路径 `builds/cft/…` 是硬编码的，
   该变量只换域名，拼到镜像上是 404。
4. **解压多套一层文件夹**：`chrome-win64\chrome-win64\` 这种结构 playwright 认不出来，
   表现为「明明放了浏览器却还在下载」。
5. **headless shell 的目录名**：必须是 `chrome-headless-shell-win64\`，
   里面的可执行文件是 `chrome-headless-shell.exe`。
   有些教程写成 `chrome-win64\headless_shell.exe`，那是错的。
6. **手动装时忘了标记文件**：路线 C 与下面的备选都要建
   `INSTALLATION_COMPLETE` **和** `DEPENDENCIES_VALIDATED`。
   前者缺了会被当成「没装过」重新下载；后者是 30 天有效的依赖校验缓存，
   过期后 playwright 会重跑校验，那时需要 `winldd-1007\PrintDeps.exe`。
7. **删掉 `ffmpeg-1011\` / `winldd-1007\`**：下次 `scrapling install` 会重新下载；
   `winldd` 缺失还可能让 Windows 上的浏览器启动直接报错。
8. **免安装版忘了设 `PYTHONPATH`**：`scrapling.exe` 自己就导不进来，
   报 `No module named 'scrapling'`。原因见路线 B 的第 ① 条。
9. **`python -m scrapling` 不可用**：该包没有 `__main__.py`，要用 `scrapling.exe`；
   但 **`python -m playwright` 是可用的**（路线 B 的 `--dry-run` 就靠它）。
10. **默认落在 `%LOCALAPPDATA%\ms-playwright`**：那是 playwright 的默认位置。
    程序仍然优先用 `<程序目录>\ms-playwright\`（只要它存在）；
    想直接用用户目录里那份，就到 **「设置 → ④ 引擎文件位置 → 浏览器目录」**
    指过去 —— **不用搬文件**。
11. **`PLAYWRIGHT_BROWSERS_PATH` 已经设过时，环境变量优先级最高**：
    程序不会覆盖它，设置窗口里的自定义目录这时不会生效（窗口里会提示）。
    想让程序听设置里的，就把那个环境变量删掉。
12. **换过目录之后**：点一下设置窗口的「刷新状态」；程序每次探测都会重新判断，
    不必重启，但**已经在跑的抓取任务**要重开才会用上新引擎。
13. **别把「抓不到目标图片」当成引擎问题**：内容平台（抖音、小红书这类）的正文图
    地址里没有 `.jpg` 这种扩展名，靠 URL 标记识别，默认的**浏览器引擎**就能抓到 ——
    换成隐身 / 动态引擎不会让图片凭空出现。若结果里全是图标 / 表情 / 头像，
    在左侧 **运行设置** 勾上 **「过滤站点素材图（图标 / 表情 / 头像）」**，
    详见 README 的
    [常见问题](README.md#q只抓到图标--表情--头像抓不到正文图)。

---

## 5. 装完之后多出来的能力

装齐之后，除了「四种引擎都能选」，还会解锁这两件事：

| 能力 | 需要什么 | 说明 |
| --- | --- | --- |
| **Cloudflare 自动绕过** | scrapling 包 + 浏览器 | 遇到**非交互** CF 挑战时程序自动改用隐身引擎重试一次，不用你守着屏幕点。交互式 Turnstile 仍需人工 |
| HTTP 快速模式的 **TLS 指纹伪装** | scrapling 包（含 `curl_cffi`） | 按 UA 版本自动挑最接近的伪装档位 |

这两项都在左上角「设置」→「① 反检测强化」里，默认开启，可以单独关掉。

> 反检测强化里另外三项（追踪器拦截 / Canvas 指纹干扰 / WebRTC 泄露防护 /
> 真实请求头）**不依赖**这里装的东西，默认的浏览器引擎就已经生效。

---

## 6. 一句话总结

> 普通使用：什么都不用装。
> 要 HTTP 快速模式 / 自适应选择器：按步骤 1 装 `scrapling[fetchers]`。
> 还要隐身 / 动态引擎：按步骤 2 **路线 A** 解压浏览器增强包到 `<程序目录>\ms-playwright\`
> （官方源慢就走路线 C 的镜像）。
> 装到别的盘了：**不用搬** ——「设置 → ④ 引擎文件位置」指过去即可。
> 卡住了：先点左上角「设置」看当前状态，再用「打开目录」核对文件到底放对没有。
